name: tests-orders

on:
  pull_request:
    paths:
      - 'orders/**'

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: cd orders && npm install && npm run test:ci
