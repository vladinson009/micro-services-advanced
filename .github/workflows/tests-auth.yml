name: tests-auth

on:
  pull_request:
    paths:
      - 'auth/**'

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: cd auth && npm install && npm run test:ci
